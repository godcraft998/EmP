local modules = {}

local RS = game:GetService("ReplicatedStorage")
local SP = game:GetService("StarterPlayer")

local RaidShopData = require(RS.Modules.Gameplay.Raids.RaidShopData)
local RaidShopData = require(SP.Modules.Interface.Loader.Gameplay.Raids.RaidShopDataHandler)

function modules.GetStock(id, name)

end

function modules.Buy(id, name, amount)

end

function modules.GetCurrency(id)

end

return modules